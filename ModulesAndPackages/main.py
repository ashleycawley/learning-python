#!/usr/bin/env python3
import sub # Imports my 'sub' module I just made up which is a 'sub.py' file in the same directory.

print(sub.data) # Prints data/string which is actually contained within the other sub.py file (or module that has been imported)